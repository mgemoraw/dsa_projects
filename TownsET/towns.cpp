#include "towns.h"




int main(void)
{
    town_t *head = NULL;

    size_t op = 0;
    // cout << format();
    do {

        print_menu();
        cin >> op;

        switch(op)
        {
            case 0:
                break;
            case 1:
                break;
            case 2:
                break;
            case 3:
                break;
            case 4:
                break;
            default:
                break;
        }
    } while (op != 0);
    return (0);
}