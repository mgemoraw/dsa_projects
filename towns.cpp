#include "towns.h"


int main(void)
{
    town_t *head = NULL;

    size_t op = 0;

    do {

        print_menu();
        cin >> op;

        switch(op)
        {
            case 0:
                break;
            default:
                break;
        }
    } while (op != 0);
    return (0);
}